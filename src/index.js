import React from "react"; //logic of react 

import ReactDOM from "react-dom";

import App from "./App";


ReactDOM.render(
     <App />, //component
     document.getElementById("root")  //place where component is to be shown
  );